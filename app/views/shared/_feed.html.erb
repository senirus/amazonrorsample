<% if @feed_items.any? %>
   <ol class="microposts">
      <% render @feed_items%>
   </ol>
   <%= will_pginate @feed_items,
                            params: { controller: :static_pages, action: :home } %>
<% end %>